add answers for bst and generators
# Please enter the commit message for your changes. Lines starting
# with '#' will be ignored, and an empty message aborts the commit.
#
# Committer: Brian Jenney <brianjenney@Brians-MacBook-Air.local>
#
# On branch secret_solutions
# Your branch is up to date with 'origin/secret_solutions'.
#
# Changes to be committed:
#	modified:   src/generators/generators.js
#	modified:   src/generators/generators.test.js
#	modified:   src/trees_and_tries/bst.js
#
# ------------------------ >8 ------------------------
# Do not modify or remove the line above.
# Everything below it will be ignored.
diff --git a/src/generators/generators.js b/src/generators/generators.js
index adf4cfa..a198860 100644
--- a/src/generators/generators.js
+++ b/src/generators/generators.js
@@ -5,7 +5,10 @@
  * @returns {iterator} ex: {value: 1, done: false}
  */
 function* forLoopGenerator(intial) {
-    // your code here
+	// your code here
+	while (intial > 0) {
+		yield intial--;
+	}
 }
 
 /**
@@ -14,10 +17,13 @@ function* forLoopGenerator(intial) {
  * @returns {iterator} ex: {value: 1231234, done: false}
  */
 function* generateRandomId() {
-    // your code here
+	// your code here
+	while (true) {
+		yield Math.floor(Math.random() * 1000000);
+	}
 }
 
 module.exports = {
-    forLoopGenerator,
-    generateRandomId,
+	forLoopGenerator,
+	generateRandomId,
 };
diff --git a/src/generators/generators.test.js b/src/generators/generators.test.js
index b0303a8..c724c2d 100644
--- a/src/generators/generators.test.js
+++ b/src/generators/generators.test.js
@@ -1,23 +1,23 @@
 const { generateRandomId, forLoopGenerator } = require('./generators');
 
 describe.skip('forLoopGenerator', () => {
-    it('initializes a loop starting at the initial number', () => {
-        const looper = forLoopGenerator(10);
+	it('initializes a loop starting at the initial number', () => {
+		const looper = forLoopGenerator(10);
 
-        expect(looper.next().value).toEqual(10);
-        expect([...looper]).toEqual([9, 8, 7, 6, 5, 4, 3, 2, 1]);
+		expect(looper.next().value).toEqual(10);
+		expect([...looper]).toEqual([9, 8, 7, 6, 5, 4, 3, 2, 1]);
 
-        expect(looper.next()).toEqual({ done: true, value: undefined });
-    });
+		expect(looper.next()).toEqual({ done: true, value: undefined });
+	});
 });
 
 describe.skip('generateRandomId', () => {
-    it('generates a random id', () => {
-        const idGenerator = generateRandomId();
-        const idMap = new Set();
-        for (let i = 0; i < 100; i++) {
-            idMap.add(idGenerator.next().value);
-        }
-        expect(idMap.size).toEqual(100);
-    });
+	it('generates a random id', () => {
+		const idGenerator = generateRandomId();
+		const idMap = new Set();
+		for (let i = 0; i < 100; i++) {
+			idMap.add(idGenerator.next().value);
+		}
+		expect(idMap.size).toEqual(100);
+	});
 });
diff --git a/src/trees_and_tries/bst.js b/src/trees_and_tries/bst.js
index c433c50..b0687c7 100644
--- a/src/trees_and_tries/bst.js
+++ b/src/trees_and_tries/bst.js
@@ -1,30 +1,71 @@
 function BST(value) {
-    this.value = value;
-    this.right = null;
-    this.left = null;
+	this.value = value;
+	this.right = null;
+	this.left = null;
 }
 
-BST.prototype.insert = function (value) {};
+BST.prototype.insert = function (value) {
+	if (value <= this.value) {
+		if (!this.left) this.left = new BST(value);
+		else this.left.insert(value);
+	} else if (value > this.value) {
+		if (!this.right) this.right = new BST(value);
+		else this.right.insert(value);
+	}
+};
 
-BST.prototype.search = function (value) {};
+BST.prototype.search = function (value) {
+	if (this.value === value) return true;
+	else if (value < this.value) {
+		if (!this.left) return false;
+		else return this.left.search(value);
+	} else if (value > this.value) {
+		if (!this.right) return false;
+		else return this.right.search(value);
+	}
+};
 
 const inorderVals = [];
 
 BST.prototype.inorder = function () {
-    // add your code
+	// add your code
+	if (this.left) {
+		this.left.inorder();
+	}
+	inorderVals.push(this.value);
+	if (this.right) {
+		this.right.inorder();
+	}
 
-    return inorderVals;
+	return inorderVals;
 };
 
 const postVals = [];
 
 BST.prototype.postorder = function () {
-    // add your code
-    return postVals;
+	// add your code
+	if (this.left) {
+		this.left.postorder();
+	}
+	if (this.right) {
+		this.right.postorder();
+	}
+	postVals.push(this.value);
+
+	return postVals;
 };
 
 BST.prototype.bfs = function () {
-    //add code here
+	//add code here
+	const q = [this];
+	const vals = [];
+	while (q.length) {
+		const node = q.shift();
+		vals.push(node.value);
+		if (node.left) q.push(node.left);
+		if (node.right) q.push(node.right);
+	}
+	return vals;
 };
 
 module.exports = BST;
